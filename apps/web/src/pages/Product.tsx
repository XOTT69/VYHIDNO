import { Bell, Check, ExternalLink, ShieldCheck, Star } from 'lucide-react'
import PriceChart from '../components/PriceChart'
import { product } from '../data/mock'
import { money } from '../lib/format'

export default function Product(){
  return <section className="container page"><div className="product-hero"><div className="product-picture">🐶</div><div className="product-info"><span className="crumb">Тварини / Корм / Monge</span><h1>{product.name}</h1><p>{product.variant}</p><div className="product-price"><strong>{money(product.price)}</strong><span className="big-score"><Star size={17} fill="currentColor"/> {product.score}/100</span></div><div className="verdict"><Check/> {product.verdict} · ціна нижча за середню на 30 днів</div><div className="product-actions"><button className="primary-btn"><Bell size={18}/> Стежити за ціною</button><button className="secondary-btn">Назвати свою ціну</button></div></div></div><div className="stats-grid"><div><span>Зараз</span><b>{money(product.price)}</b></div><div><span>Середня 30 днів</span><b>{money(product.avg30)}</b></div><div><span>Мінімум 6 міс.</span><b>{money(product.low180)}</b></div><div><span>VYHIDNO Score</span><b>{product.score}/100</b></div></div><div className="two-col"><div className="card"><div className="card-head"><div><span>Історія</span><h2>Як змінювалась ціна</h2></div><div className="range-tabs"><button>30д</button><button className="active">90д</button><button>6м</button></div></div><PriceChart values={product.history}/></div><div className="card"><div className="card-head"><div><span>Пропозиції</span><h2>Де купити</h2></div></div><div className="offer-list">{product.offers.map((o,i)=><div className={i===0?'offer best-offer':'offer'} key={o.store}><div><b>{o.store}</b><span><Star size={13} fill="currentColor"/> {o.rating} · {o.delivery}</span></div><div><strong>{money(o.price)}</strong><button aria-label="Відкрити магазин"><ExternalLink size={17}/></button></div></div>)}</div><div className="trust-note"><ShieldCheck size={18}/> Sponsored-пропозиції ніколи не підміняють найвигіднішу ціну.</div></div></div></section>
}
